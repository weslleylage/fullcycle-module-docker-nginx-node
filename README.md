# Desafio Full Cycle modulo Docker Nginx|Node|Mysql

Para executar o projeto:
```shell
docker-compose up --build -d
```

rotas:
```shell
http://localhost:8080/
http://localhost:8080/delete/{id}
http://localhost:8080/deleteAll
```